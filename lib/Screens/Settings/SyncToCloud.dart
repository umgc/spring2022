import 'package:flutter/material.dart';

class SyncToCloud extends StatefulWidget {
  @override
  SyncToCloudState createState() => SyncToCloudState();
}

class SyncToCloudState extends State<SyncToCloud> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
          child: Text("SYNC TO CLOUD ITEMS TO BE DEVELOPED"),
        )
    );
  }
}